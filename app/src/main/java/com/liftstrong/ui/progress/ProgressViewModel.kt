package com.liftstrong.ui.progress

import androidx.lifecycle.ViewModel
import dagger.hilt.android.lifecycle.HiltViewModel
import javax.inject.Inject

/**
 * ViewModel for the Progress screen.
 * 
 * This ViewModel handles the business logic for the ProgressFragment,
 * including fetching and processing progress data for visualization.
 */
@HiltViewModel
class ProgressViewModel @Inject constructor(
    // Repositories will be injected here
) : ViewModel() {
    
    // LiveData and methods will be added as needed
    
}
